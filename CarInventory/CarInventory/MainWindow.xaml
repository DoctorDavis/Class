<Window x:Class="CarInventory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CarInventory"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="800">
    <StackPanel Margin="10">
        <TextBlock Text="Car Inventory" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <Button Content="Add Car" Width="100" Height="30" Margin="0,0,0,10" Click="AddCarButton_Click"/>

        <ListView Name="CarListView" Height="600">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Make" Width="120" DisplayMemberBinding="{Binding Make}"/>
                    <GridViewColumn Header="Model" Width="120" DisplayMemberBinding="{Binding Model}"/>
                    <GridViewColumn Header="Year" Width="80" DisplayMemberBinding="{Binding Year}"/>
                    <GridViewColumn Header="Price" Width="80" DisplayMemberBinding="{Binding Price}"/>
                </GridView>
            </ListView.View>
    
</Window>
